<div class="joplin-table-wrapper"><table><tbody><tr><th><p><strong>Mô tả</strong></p></th><th><p>Cho phép Người dùng hoặc Quản trị viên xuất danh sách thẻ từ vựng trong một chủ đề (Topic) ra file Excel (.xlsx) để lưu trữ hoặc nhập danh sách thẻ từ vựng mới từ file Excel vào chủ đề tương ứng theo các chế độ linh hoạt (append, replace, upsert).</p></th></tr><tr><td><p><strong>Tác nhân kích hoạt</strong></p></td><td><p>Người dùng hoặc Quản trị viên</p></td></tr><tr><td><p><strong>Tiền điều kiện</strong></p></td><td><ul><li>Tác nhân đã đăng nhập vào tài khoản hợp lệ trên hệ thống.</li><li>Đối với Người dùng: Phải là chủ sở hữu của bộ thẻ (User Deck) chứa chủ đề (Topic) cần nhập/xuất từ vựng và đã truy cập vào trang Chi tiết bộ thẻ cá nhân.</li><li>Đối với Admin: Đang ở trang quản lý từ vựng hệ thống.</li></ul></td></tr><tr><td><p><strong>Các bước thực hiện</strong></p></td><td><p><strong>Luồng A: Xuất file từ vựng (Export):</strong></p><ol><li>Tác nhân nhấn nút "Nhập / Xuất từ vựng".</li><li>Hệ thống hiển thị Modal Nhập/Xuất với 2 tab: "Xuất từ vựng" (mặc định), "Nhập từ vựng"</li><li>Tác nhân nhấn nút "Xuất ra file Excel".</li><li>Hệ thống tự động tải file về thiết bị của tác nhân dưới tên vocab_{topicName}_export.xlsx.</li><li>Hệ thống hiển thị thông báo thành công: "Xuất dữ liệu thẻ thành công".</li></ol><p><strong>Luồng B: Nhập file từ vựng (Import):</strong></p><ol><li>Tác nhân mở Modal Nhập/Xuất và nhấn chuyển sang Tab "Nhập từ vựng".</li><li>Tác nhân có thể nhấn vào liên kết "Tải file Excel mẫu" để tải về file Excel mẫu đã được cấu hình sẵn các cột header chuẩn.</li><li>Tác nhân chọn một trong các Chế độ nhập: append (mặc định), replace, upsert.</li><li>Tác nhân kéo và thả file Excel đuôi .xlsx từ máy tính vào khu vực tải lên, hoặc nhấp chuột để chọn tệp tin từ thiết bị. Hệ thống kiểm tra định dạng và hiển thị tên tệp cùng dung lượng file đã chọn.</li><li>Tác nhân nhấn nút "Bắt đầu nhập từ vựng" (chỉ enable khi Tác nhân đã nhập file vào khu vực upload)</li><li>Hệ thống gửi file đã đính kèm lên server xử lý, server kiểm tra tính hợp lệ của từng dòng và tiến hành ghi nhận dữ liệu vào cơ sở dữ liệu tương ứng với chế độ import đã chọn.</li><li>Server trả về kết quả thống kê chi tiết của lượt import (số lượng thành công, thất bại, danh sách dòng lỗi).</li><li>Hệ thống hiển thị thông báo "Nhập từ vựng thành công!" kèm thống kê: Tổng số dòng xử lý, Đã thêm mới, Đã cập nhật, Đã bỏ qua, Bị lỗi.</li></ol><ul><li>Nếu file Excel tải lên thiếu một trong các cột header bắt buộc hay thiếu dữ liệu. Hệ thống hiển thị lỗi: "Có lỗi xảy ra khi nhập từ vựng từ file." kèm Chi tiết lỗi từng dòng</li></ul></td></tr><tr><td><p><strong>Các luồng phụ khác</strong></p></td><td><ul><li>Huỷ bỏ thao tác: Tác nhân có thể nhấn nút "Huỷ" hoặc click ra ngoài vùng Modal để đóng giao diện Nhập/Xuất mà không lưu lại bất cứ thay đổi nào đang chọn.</li><li>Xoá file đã chọn: Sau khi chọn file thành công nhưng muốn thay thế bằng file khác trước khi nhấn gửi, Tác nhân nhấn vào nút hình dấu nhân (x) cạnh tên file để xoá file đã chọn và đưa vùng tải lên về trạng thái ban đầu.</li></ul></td></tr></tbody></table></div>