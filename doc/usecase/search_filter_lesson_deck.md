<div class="joplin-table-wrapper"><table><tbody>
<tr><th><p><strong>Mô tả</strong></p></th><th><p>Cho phép khách vãng lai và người dùng thu hẹp danh sách bài học và bộ từ vựng đang hiển thị bằng cách nhập từ khóa tìm kiếm hoặc áp dụng các bộ lọc theo trình độ CEFR, chủ đề. Các điều kiện lọc có thể kết hợp đồng thời. Kết quả cập nhật ngay sau mỗi thay đổi bộ lọc.</p></th></tr>
<tr><td><p><strong>Tác nhân kích hoạt</strong></p></td><td><p>Khách vãng lai và người dùng</p></td></tr>
<tr><td><p><strong>Tiền điều kiện</strong></p></td><td><p>Hệ thống hoạt động bình thường ở các chức năng "Bài học" và "Từ vựng".</p></td></tr>
<tr><td><p><strong>Các bước thực hiện</strong></p></td><td><ol>
<li>Người dùng nhập từ khóa vào ô tìm kiếm ("Tìm lesson theo tiêu đề..." hoặc "Tìm bộ từ theo tiêu đề..."). Hệ thống tự động cập nhật danh sách, hiển thị các mục có tiêu đề hoặc mô tả chứa từ khóa (không phân biệt chữ hoa, chữ thường).</li>
<li>Người dùng nhấn vào một mức trình độ CEFR (A1, A2, B1, B2, C1, C2) để lọc. Hệ thống cập nhật danh sách chỉ hiển thị các mục thuộc trình độ đã chọn. Nhấp lại vào cùng trình độ để bỏ lọc.</li>
<li>Người dùng nhấn vào một chủ đề từ danh sách chủ đề để lọc. Hệ thống cập nhật danh sách chỉ hiển thị các mục thuộc chủ đề đã chọn.</li>
<li>Người dùng có thể kết hợp đồng thời từ khóa, trình độ và chủ đề. Hệ thống hiển thị các mục thỏa mãn tất cả điều kiện đang được áp dụng. Nếu không có mục nào khớp, hệ thống hiển thị thông báo:
<ul>
<li>"Không tìm thấy bài học nào phù hợp với bộ lọc hiện tại."</li>
<li>"Không tìm thấy bộ từ nào phù hợp với bộ lọc hiện tại."</li>
</ul></li>
</ol></td></tr>
</tbody></table></div>
