<div class="joplin-table-wrapper"><table><tbody><tr><td><p><strong>Mô tả</strong></p></td><td><p>Cho phép người dùng (Admin hoặc User) tải lên hình ảnh từ máy tính. Hình ảnh được dùng làm ảnh bìa bộ từ (cover image), ảnh minh họa thẻ từ (card image), ảnh thumbnail bài học (lesson thumbnail) hoặc ảnh đại diện người dùng (avatar).</p></td></tr><tr><td><p><strong>Tác nhân kích hoạt</strong></p></td><td><p>Quản trị viên hoặc Người dùng</p></td></tr><tr><td><p><strong>Tiền điều kiện</strong></p></td><td><ul><li>Người dùng đã đăng nhập vào hệ thống.</li><li>File ảnh phải thuộc một trong các định dạng cho phép: JPEG, PNG, WebP.</li><li>Kích thước file không vượt quá 5MB.</li></ul></td></tr><tr><td><p><strong>Các bước thực hiện</strong></p></td><td><ol><li>Người dùng truy cập vào một form có chức năng upload ảnh, bao gồm một trong các trường hợp sau:</li></ol><ul><li>Tạo/Sửa bộ từ (Admin Deck Create/Edit): khu vực “Ảnh bìa” (cover image) với ô chọn file ảnh.</li><li>Tạo/Sửa thẻ từ (Admin Card Create/Edit): khu vực “Hình ảnh từ vựng” (card image) với ô chọn file ảnh.</li><li>Tạo/Sửa bài học (Admin Lesson Create/Edit): khu vực “Ảnh thumbnail” (lesson thumbnail) với ô chọn file ảnh.</li><li>Trang hồ sơ cá nhân (Profile Page): khu vực ảnh đại diện (avatar) với ô chọn file ảnh.</li></ul><ol><li>Hệ thống hiển thị nút “Tải ảnh lên” (Upload Image) / “Đổi ảnh” (Change Image) và gợi ý định dạng cho phép (PNG, JPG, WebP, tối đa 5MB).</li><li>Người dùng nhấn vào nút “Tải ảnh lên” và chọn một file ảnh từ máy tính.</li><li>Hệ thống kiểm tra:</li></ol><ul><li>Chỉ hiển thị và cho phép chọn các file ảnh có định dạng PNG, JPG, WebP.</li><li>Kiểm tra kích thước file không vượt quá 5MB. Nếu vượt quá, hiển thị thông báo lỗi “File quá lớn”.</li></ul><ol><li>Nếu file hợp lệ, hệ thống chuyển nút “Tải ảnh lên” sang trạng thái “đang tải” và vô hiệu hóa nút để tránh thao tác trùng lặp. Trong quá trình upload, nếu người dùng đóng form hoặc điều hướng sang trang khác, quá trình upload bị hủy bỏ và không có thay đổi nào được lưu.</li><li>Hệ thống hiển thị ảnh mới. Nếu có lỗi, hiển thị thông báo lỗi tương ứng.</li></ol></td></tr></tbody></table></div>